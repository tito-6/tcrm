# Python Dependencies for CRM Lead Integration System
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.28.0
xmlrpc>=1.0.1

# WhatsApp Business API dependencies
phonenumbers>=8.12.0

# Development and testing
python-dotenv>=0.19.0

# Optional: For enhanced functionality
Pillow>=9.0.0  # For image processing if needed
lxml>=4.6.0    # For XML processing

# Note: This file is for reference only
# The actual Python dependencies are managed by the Odoo Docker container
# These are the external Python packages used by the custom modules