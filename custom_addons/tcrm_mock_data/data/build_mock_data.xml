<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Build (or refresh) the shared "Nova Estates" mock-data scenario.
        `build_all` is idempotent (keyed on ir.model.data external IDs), so it is
        safe to run on every install and upgrade of this module.
    -->
    <function model="tcrm.mock.builder" name="build_all"/>
</odoo>
