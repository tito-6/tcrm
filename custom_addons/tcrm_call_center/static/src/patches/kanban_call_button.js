/** @odoo-module **/

/**
 * Kanban "Ara" is provided via view inheritance (type=object button).
 * This module exists so the asset bundle always loads Santral CRM helpers
 * and documents the convention for Lead Havuzu cards.
 */
export const SANTRAL_KANBAN_CALL_CLASS = "o_santral_kanban_call";
